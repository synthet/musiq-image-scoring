You are reviewing one already-clustered image stack/substack from a photo culling system.

The group has fewer than 10 images. Some images are marked picked, rejected, or neutral. Your task is to decide whether each currently rejected image is safely redundant enough to mark as a metadata-only removal candidate.

Editorial model (aligned with carousel-pick curation):
- Scores and model_scores (0–1 ML signals) inform your shortlist; visual duplicate and pose-diversity rules override pure score rank.
- A lower score_general alone is not enough to remove if vision shows a unique pose, subject moment, or composition.
- On tight bursts (same subject/perch/light): favor diverse poses among picked images; remove rejected frames that are near-duplicates of a stronger picked alternative (profile vs frontal vs opposite profile).
- Every remove reason must cite filename, relevant scores (including clip_quality_v0 when present), what you saw in the thumbnail, and better_alternatives from picked ids.

Mandatory vision before any "remove":
- Open every file path in thumbnail_manifest using your file-view tool before deciding.
- Map each path to image_id from the manifest.
- If a thumbnail is unreadable, set decision "uncertain" or "keep" and add risk_flags "unreadable_preview".
- Set vision_used true and list every image_id whose thumbnail you opened in viewed_image_ids.

Be conservative. Return "remove" only when the rejected image is clearly worse than picked alternatives and has no unique subject, species, pose, behavior, composition, documentary value, or technical advantage.

Return JSON only. Do not use markdown.

Never recommend removal when:
- there are no picked images
- group confidence or per-image confidence is low
- the rejected image is visually unique
- the rejected image has a unique species/keyword/subject
- the rejected image has better technical quality than picked alternatives (score_technical, score_general, or clip_quality_v0)
- the rejected image is the only usable version of a moment/action
- better alternatives are missing or not picked
- you did not view the thumbnail and cannot confirm redundancy
- you are unsure

When picked image count is lower than rejected image count, be especially conservative; default to keep or uncertain unless redundancy is obvious visually.

Use only image ids from the provided packet.
Use only picked image ids as better_alternatives.
Return a decision for every rejected image.
Label images by filename in reasons.

Response schema_version must be "agent-cull-response-v1".
group_decision must be "apply_removals" or "do_not_apply".
Include optional vision_used (boolean) and viewed_image_ids (integer array).
Each rejected_image_decisions entry must include image_id, filename, decision (remove|keep|uncertain), confidence 0..1, reason, better_alternatives, risk_flags.

Review packet JSON follows:
