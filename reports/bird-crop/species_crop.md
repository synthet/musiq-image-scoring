# BioCLIP species — crop vs whole image

## 1. Is the candidate list the bottleneck?

Checked first because a zero-shot classifier can only return a name from its candidate list. If the birds in the library are not on the list, no amount of crop tuning can produce a right answer.

> ⚠️ **This check cannot answer that question, and the numbers below are easy to over-read.** Labels are produced by BioCLIP *from* this list, so they cannot be used to validate it — the same circularity that rules out `images.rating` as ground truth. Treat this as a description of the label distribution, not as evidence.

- Candidate list: **360** names (`data/bird_species_list.txt`)
- Distinct species assigned across **51766** tags: **360**
- Candidates never assigned: **0** (expected to be ~0 by construction; not a coverage signal)
- Top-20 species share: **39.3%** · normalised entropy **0.8569** (near 1.0 = mass spread ~uniformly, consistent with guessing; well below 1.0 = concentrated, consistent with real signal)

> NOT evidence that the candidate list matches the library. Labels are generated by BioCLIP from this list, so both 'every tag is on the list' and 'every candidate got used' are expected by construction — the second especially, since argmax over 360 classes across 51766 tags will touch every class even when guessing. Read normalised_entropy instead: near 1.0 means the mass is spread almost uniformly across the list (consistent with guessing), well below 1.0 means it concentrates on particular species (consistent with real signal). Correctness still requires the human label set or an expert check.

Most-assigned species:

  - snowy egret — 1904
  - northern cardinal — 1844
  - carolina wren — 1426
  - roseate spoonbill — 1426
  - white ibis — 1267
  - red-bellied woodpecker — 1256
  - great egret — 979
  - great blue heron — 973
  - little blue heron — 972
  - summer tanager — 963

## 2. Crop vs whole image

54 bursts / 236 images, 360 candidate species.

| Subject size | Agreement (crop) | Agreement (whole) | Confidence (crop) | Confidence (whole) | Flip rate | bursts |
|---|---|---|---|---|---|---|
| smallest | 0.9373 | 0.8844 | 0.8728 | 0.7717 | 0.3 | 18 |
| middle | 0.9253 | 0.9563 | 0.8736 | 0.8504 | 0.1358 | 18 |
| largest | 0.8889 | 0.8565 | 0.7418 | 0.7492 | 0.2 | 18 |

**Overall** — agreement 0.9172 (crop) vs 0.8991 (whole), delta **0.0181**; confidence 0.8314 vs 0.7916, delta **0.0398**; labels flip on **0.2119** of images.

> Higher within-burst agreement is better: frames of the same bird seconds apart should get one name. Confidence is a softmax over the candidate list, so it measures how peaked that distribution is, NOT correctness — a confidence drop with an agreement rise is consistent with the crop removing background cues the classifier was over-trusting. Neither metric can establish accuracy; only the human label set or an expert check can.

---

Generated by `scripts.research.bird_crop.species_crop_eval`. Production was read read-only; nothing was written to it.
