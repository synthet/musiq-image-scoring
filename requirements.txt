# MUSIQ: Multi-scale Image Quality Transformer - Minimal Dependencies
# CPU-only installation for production use

# Core ML framework (CPU-only) — 2.15.1 fixes CVE-2024-27314
tensorflow-cpu>=2.15.1,<2.16.0

# Image processing
Pillow>=10.4.0,<11.0

# Numerical computing
numpy>=1.24.4,<2.0

# Optional: TensorFlow Hub for model loading
tensorflow-hub>=0.16.1,<0.17

# Kaggle Hub for advanced model loading (KONIQ, VILA)
kagglehub>=0.3.4,<0.4

# keyword extraction deps
rawpy>=0.21.0,<1.0
exifread>=3.0.0
imageio>=2.34.0,<3.0
tifffile>=2023.7.0,<2026.0
gradio>=5.0,<6.0
scikit-learn>=1.3.0,<2.0
umap-learn>=0.5,<1.0

# MCP Server (optional - for Cursor debugging integration)
mcp>=1.0.0,<2.0

# PostgreSQL database driver (required for db.py)
psycopg2-binary>=2.9,<3.0

# pgvector Python client (vector type support for psycopg2)
pgvector>=0.2.0,<1.0

# Database migration framework
alembic>=1.13.0,<2.0

# LIQE model dependencies (PyTorch)
pyiqa>=0.1.14
torch>=2.0.0
torchvision>=0.15.0

# CLI (cli.py) — Typer + Rich
typer>=0.24.0
rich>=14.0.0

# Bird detection (optional) — YOLO crop step before BioCLIP species classification.
# Installed in the GPU/ML venv (~/.venvs/tf); modules/bird_detection.py fails open when absent.
ultralytics>=8.0.0
huggingface_hub>=0.20.0
