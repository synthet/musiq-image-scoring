{
  "_comment": "User-level ~/.cursor/mcp.json only — cross-repo tools (github, subagent-orchestrator). Do NOT put fff here (needs project git cwd). Use each repo's .cursor/mcp.json with fff-be / fff-gallery.",
  "mcpServers": {}
}
