# Minimal dependency set for the blocking GitHub Actions gates.
#
# Deliberately excludes TensorFlow, Torch and the CUDA wheels: the fast smoke
# slice (scripts/ci/run_fast_smoke_tests.sh) and the API contract gate
# (scripts/ci/contract_check.py) import modules.api / modules.db but never touch
# the ML runtimes, so a hosted runner installs this in seconds instead of pulling
# several GB of GPU wheels.
#
# Versions track requirements/requirements_wsl_gpu.txt so CI resolves the same
# stack as the app. When you bump a pin there, bump it here too.
#
# Anything needing the ML stack belongs in the coverage workflow, which installs
# the full requirements file instead. See docs/testing/CI_GATES.md.

fastapi==0.135.1
pydantic==2.12.3
numpy==2.1.3
pillow==11.3.0
PyYAML==6.0.3
httpx==0.28.1

pytest>=8,<10
