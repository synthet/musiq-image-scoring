{
  "_comment": "Antigravity: copy to mcp_config.json (gitignored) or merge into Antigravity MCP settings. cwd is repo root. No separate playwright server — use browser.* via is-be-mcp dispatch.",
  "mcpServers": {
    "is-be-mcp": {
      "command": "node",
      "args": ["mcp-server/dist/compactIndex.js"],
      "cwd": "."
    }
  }
}
